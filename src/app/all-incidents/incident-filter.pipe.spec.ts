import { IncidentFilterPipe } from './incident-filter.pipe';

describe('IncidentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IncidentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
